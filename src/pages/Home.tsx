import { Hero } from '../components/Hero';
import { TechSection } from '../components/TechSection';

export const Home = () => {
    return (
        <main>
            <Hero />
            <TechSection />
        </main>
    );
};
